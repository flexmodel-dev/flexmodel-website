---
sidebar_position: 1
---

# 介绍

## 为什么选择 Flexmodel？

AI 降低了代码的编写成本，却无法阻止维护成本的指数级飙升。当复杂业务缺乏绝对约束，自由散漫的高代码只会加速系统熵增。

Flexmodel 以**统一规范**构筑全局底座，从源头消灭技术黑盒——将数据建模、接口生成、权限管控、流程编排沉淀为标准化的平台能力，让每一条业务规则都透明可控。这才是
AI 时代的正确解法。

### Flexmodel 具备哪些功能？

1. API 服务：基于 GraphQL 自动生成 REST API，运行时动态构建，无需手写接口代码
2. 数据源管理：适配 10+ 主流及国产数据库（MySQL、PostgreSQL、Oracle、达梦、TiDB 等）
3. 数据建模：可视化模型定义，字段级约束、索引与关联关系管理
4. 身份认证：完整 RBAC 权限体系，支持 OIDC / LDAP / 社交账号 / JWT，项目级身份提供商
5. 流程编排：BPMN 风格可视化流程设计器，后端业务逻辑低代码编排
6. 任务调度：内置作业执行引擎，支持 Cron、延时及事件驱动调度
7. 对象存储：统一抽象 S3 / OSS / 本地存储，Bucket 与文件对象可视化管理
8. MCP 协议：基于 MCP 开放核心工具，让 AI 客户端直接管理项目与数据
9. 云函数：Deno Functions Runtime + Worker 隔离执行，TypeScript 原生支持（实验性）
10. Native 编译：GraalVM Native Image 支持，毫秒级启动，内存占用低于 60MB

### Flexmodel 能做什么？

- **统一数据服务** — 将通用接口标准化，前端直接消费，消除重复开发
- **BFF / API 网关** — 将后端服务转化为 GraphQL 或 REST API，灵活适配前端需求
- **低代码数据引擎** — 数据模型一键生成数据服务，赋能低代码页面快速搭建
- **AI 时代底座** — 以规范约束替代代码自由，让 AI 生成的内容运行在可控边界内

![](comparison.png)

Flexmodel 目前仍在积极开发和完善中，欢迎开发者朋友提出宝贵意见，共同交流与成长。
